# Optional. Leave blank to use the default AWS credential chain
# (environment variables, shared config, or SSO).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1

# Ember API
EMBER_HOST=127.0.0.1
EMBER_PORT=8000
